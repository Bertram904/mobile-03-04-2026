Place your custom notification sound file here.

Required file name for current Task 2 setup:
- `alarm_chill.wav`

Notes:
- Do not use a remote URL directly for notification sound.
- Download audio from a link, convert to `.wav`, then save as `alarm_chill.wav`.
- After changing sound files, rebuild the app:
  - Android/iOS dev build: `npx expo run:android` or `npx expo run:ios`
  - EAS build also works.
